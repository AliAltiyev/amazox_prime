name: data
description: data module.
version: 0.0.1
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=1.17.0'

dependencies:
  #base
  json_annotation: ^4.8.1

  flutter:
    sdk: flutter
  domain:
    path: ../domain
  core:
    path: ../core

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  json_serializable: ^6.7.0
  build_runner: ^2.4.5
  hive_generator: ^2.0.0

flutter:
